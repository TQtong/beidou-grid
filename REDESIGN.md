# 北斗空域立方体场渲染 · 重设计说明

## 目标

参考 Xt3d 的 GPU 实例化思路，对 beidou-grid 的网格渲染层做一次重设计，使其在满足三项核心能力的同时具备渲染海量立方体的性能上限：其一，渲染北斗网格的网格线与每一个立方体，默认以线框呈现，可整体切换为实心填充，并支持点击单个立方体循环改变其颜色；其二，随地图比例尺动态调整北斗网格的剖分级别；其三，模拟无人机低空飞行，实时标记并着色其所在格与周边受影响的立方体。

## 总体架构

重设计在原有领域层（GeoSOT 编解码）与渲染基础设施（区域解析、比例尺选级、剖分、私有 API 封装）之上，新增一条以「统一实例化立方体场」为核心的渲染路径，与既有的线格网/标注/隔离路径并存而不互相干扰。新路径分为三层：核心层负责 GPU 资源与渲染命令，控制层负责无人机与编排，应用层负责 Viewer 生命周期与界面。

核心层的中心是立方体场。它将整片网格视为一组规则排布的单位立方体实例：每个立方体在网格中的位置完全由「原点加索引乘步长」确定，因此无需向 GPU 上传任何逐实例的位置数据，顶点着色器仅凭实例编号即可在着色器内反解出三维网格索引，并在区域原点的本地东北天坐标系中算出该立方体的世界位置。这一「零位置上传」的策略把每个立方体的常驻显存压缩到极低水平，是支撑百万级立方体在一次绘制命令内完成渲染的关键。

立方体的状态以单字节类别表达，统一存放在一张以行优先顺序铺排的状态纹理中；另有两张调色板纹理分别服务线框与填充两种模式，因为两种模式对「空格」的透明度诉求不同。着色时，顶点或片元着色器以实例编号定位状态纹素、取出类别、再到调色板取色，整个着色查找链路都在 GPU 内闭环完成。线框与填充共享同一套立方体角点缓冲，仅通过切换索引缓冲、图元类型与渲染状态实现模式切换，无需重建任何 GPU 资源。

点击改色依托一套自管理的离屏拾取流程。拾取时在离屏帧缓冲中以实心立方体重绘一遍，将每个立方体的编号编码进颜色输出，再回读鼠标位置处的像素解码出被点中的立方体。为规避手动执行渲染命令时引擎自动矩阵与当前相机不同步的问题，拾取链路使用由 CPU 端自行相乘得到的模型视图投影矩阵作为统一变量，并在合法的渲染上下文与当前相机状态下、于每帧更新阶段内执行，全程异常隔离，任何拾取失败都不会冲垮主渲染循环。被点中的立方体按「空、A、B、C、空」的顺序循环切换颜色，且选中状态以网格中心的地理坐标为键持久化保存，使其在相机平移引发的重建中得以精确保留，在级别变化时则自动剔除不再匹配的项。

## 比例尺联动

级别联动由编排器在相机停稳后触发，并经动画帧防抖合并。编排器先解析当前可视范围与给定范围的交集，再依据相机比例尺选出目标级别，随后在自动模式下按格数预算做必要的级别降级，最后完成剖分并写入立方体场。为避免「高度层数依赖级别、级别又受层数影响」的循环依赖，高度方向采用固定步长，使层数有界且可预先用于预算估算。手动锁定级别时则尊重用户选择，跳过预算降级。

## 无人机模拟

无人机由控制层独立驱动。机体沿圆形巡航航线低空飞行，并叠加轻微的高度起伏以增强真实感；机体与渐隐航迹折线以场景实体方式呈现，其位置通过回调属性暴露给引擎，从而随相机以高帧率平滑刷新。机体位置与影响半径被持续喂给立方体场，由其定位机体所在格并以球形距离判定周边受影响的格，所在格标记为核心、周边标记为影响并分别着色。为兼顾观感与大网格下整幅状态纹理重传的开销，机体本身保持高帧率移动，而影响范围的重算与纹理上传按固定时间步节流。无人机的标记在显示优先级上压过用户选中，离场后再恢复底层状态。

## 取舍

为满足「每一个立方体均可独立着色」这一诉求，本设计以每个立方体为独立实例渲染，相较于仅渲染共享棱边的线格网会占用更多绘制量；这是换取逐格状态与逐格拾取能力的必要代价，并由「零位置上传 + 单命令实例化 + 预算降级」共同将其控制在可接受范围内。立方体在水平方向的尺寸由区域原点纬度统一换算，属于区域内的切平面近似，在中低纬度与常规视野尺度下足够精确。高度方向采用固定步长，换取层数有界与预算可估，代价是高层级下竖向格的高宽比可能偏大；如需更贴合真实空域分层，可在此基础上替换为按级别或按空域规则定义的非均匀分层。
